# 2026-03-07 AsheDism (Claude Code)

## 1. 並行処理によるAI提案の実装

> 一旦その案で実装してみてください。

→ suggest-init（セッション作成+タグ取得）+ suggest-one×3並行のアーキテクチャに変更
→ プログレッシブUI実装（完了した提案から順にフェードイン表示）
→ スケルトンローディング追加

## 2. バリエーションヒントのランダム化

> 並行処理している3つの予定が被っているため、シート値をランダムにしてください。

→ 各並行リクエストに「方向性:〇〇」のバリエーションヒントを追加
→ 定番・人気スポット / 穴場・ユニーク体験 / のんびり・リラックス系 の3種をハードコード

## 3. バリエーションヒントのDB管理化

> バリエーションを定数で持つのは嫌なので、データベースにたくさんのバリエーションを保存しておき、その中からランダムに3つ選ぶようにしてください。

→ variation_hintsテーブル新規作成（25個のシードデータ）
→ suggest-initでDBからランダム3件取得してフロントに返却
→ マイグレーション 00010_create_variation_hints.sql 作成

## 4. カテゴリ名の日本語化（プロンプト改善）

> AIから返ってくる予定がカテゴリニュースが入ってなさそうな気がするのですが、調査してください。

→ 原因: プロンプトにカテゴリが英語slug（food等）で渡されていた
→ prompt.ts新規作成: buildSystemPrompt / buildUserPrompt を共通化
→ カテゴリslugを日本語ラベル（食事・グルメ等）に変換してプロンプトに含める
→ gemini.ts / openai.ts の重複buildSystemPromptを削除し共通モジュールに統一

## 5. プロンプト履歴ルールの追加

> ルールを追加してください。プロンプトディレクトリ配下にプロンプトの履歴を必ず書くようにしてください。これはプロンプトを投げた際、必ず実施する処理としてください。今までのプロンプト履歴も更新してください。

→ instructions.mdに「プロンプト投入時の履歴記録」を必須ルールとして追加
→ 2026-03-06のプロンプト履歴（#9〜#14）を追記
→ 2026-03-07のプロンプト履歴を新規作成

## 6. 画像フォールバック対応

> 実際の画像取得ができない場合は、イメージ画像を取得してください。

→ unsplash.tsに3段階フォールバック検索を実装:
  1. AIが返した具体的なクエリ（場所名等）で検索
  2. 見つからなければカテゴリ別の汎用キーワードで再検索（food→"delicious food restaurant"等）
  3. それでもなければクエリの先頭2語で再検索
→ suggest-one / suggest / replan の全ルートにcategorySlugを渡すよう更新

## 7. 画像が表示されないパターンの調査

> 添付画像の通り、画像が出力されていないパターンがあります。これの原因を確認してください。

→ unsplash.tsにデバッグログ（ステータスコード、レート制限残数、検索結果なし等）を追加
→ 原因候補: Unsplash APIレート制限（50req/h）、検索結果0件、タイムアウト
→ ユーザーに再度提案生成してログ確認を依頼
→ Unsplash APIレート制限(403/429)時に5分間リクエスト停止するロジック追加済み
→ 画像が取得できない場合のUIプレースホルダー実装: カテゴリ別グラデーション背景+アイコン表示
  - 受諾済みカード（acceptedビュー）と展開カード（expandedビュー）の両方に適用
  - 画像なしでも視覚的に違和感のないデザインに改善

## 8. 画像出力改善案のキャンセル

> 検索エンジンから画像を取得してからUnsplashにフォールバックする案、およびGemini画像生成での補完案について検討

→ Google Custom Search API / Gemini画像生成の案を提示
→ ユーザー判断でキャンセル。現状のUnsplash + グラデーションプレースホルダーを維持
